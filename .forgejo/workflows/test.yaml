name: Test

on:
  push:
  pull_request:
  workflow_call:

jobs:
  pytest:
    runs-on: codeberg-tiny
    strategy:
      matrix:
        python-version:
          - "3.10"
          - "3.11"
          - "3.12"
          - "3.13"
    steps:
    - uses: actions/checkout@v4
    - uses: https://github.com/astral-sh/setup-uv@v6.5
      with:
        python-version: ${{ matrix.python-version }}
    - run: uv sync --all-extras
    - run: uv run pytest

  ruff:
    runs-on: codeberg-tiny
    steps:
    - uses: actions/checkout@v4
    - uses: https://github.com/astral-sh/ruff-action@v3

  ruff-format:
    runs-on: codeberg-tiny
    steps:
    - uses: actions/checkout@v4
    - uses: https://github.com/astral-sh/ruff-action@v3
      with:
        args: "format --check --diff"
